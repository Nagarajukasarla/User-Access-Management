document.addEventListener("DOMContentLoaded", () => {
    const approveBtn = document.querySelector(".approval-btn");
    const rejectBtn = document.querySelector(".reject-btn");
    // need to check which button user have clicked and it should be send in response
})